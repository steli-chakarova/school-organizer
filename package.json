{
  "name": "school-organizer",
  "version": "1.0.0",
  "description": "School Organizer Django Application",
  "scripts": {
    "postinstall": "playwright install --with-deps chromium",
    "build": "playwright install --with-deps chromium"
  },
  "dependencies": {
    "playwright": "1.55.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}